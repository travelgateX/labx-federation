{
  "name": "hotels",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "prestart": "rm -rf ./dist && tsc",
    "start": "node ./dist/main.js",
    "start:dev": "node --inspect ./dist/main.js",
    "start:debug": "./node_modules/nodemon/bin/nodemon.js -e ts --exec \"npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.7.2"
  }
}